import React from 'react';
import ReactDOM from 'react-dom';
import './interfaces/App.css';
import App from './interfaces/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);